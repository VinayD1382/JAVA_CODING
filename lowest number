class Main{
    public static void main(String[] args){
        int[] arr = {4,5,3,2,1,6,7,8,9};
        int min = arr[0];
        for(int i=0;i<arr.length;i++){
            if(arr[i]<min){
                min = arr[i];
            }
        }
        System.out.print(min);
    }
}
